export const BannerTitle = ({ title }) => {
    return (
      <h1 className={`banner-title`}>
        {title}
      </h1>
    )
  }
  export const Title = ({ title }) => {
    return <h1 className={`btitle`}>{title}</h1>
  } 

  